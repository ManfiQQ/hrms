{{--
    Documents — §7.1: PRESENT AS A TAB, AND EMPTY.

    ⚠ It states the absence rather than listing files nothing can open. adr/0012 decision 11
    binds the serving controller, the routes, the FormRequests, the Actions and the
    submissions table to the PR that builds this tab, and that PR is not the one that built
    the other seven.

    ⚠ The list screen's rule — a row linking to a 404 is worse than a row that does not link —
    does not reach here, and the difference is what the element PROMISES. A link promises a
    destination; a tab explaining its own absence promises nothing.

    ⚠ No PHOTO is rendered anywhere on this screen, including Personal. A photo is a file
    (adr/0013 decision 7) and displaying one pulls in the same serving path.
--}}
<div class="rounded-lg border border-dashed border-slate-300 bg-slate-50 px-4 py-10 text-center">
    <p class="text-sm font-medium text-slate-700">Documents are not available on this screen yet.</p>
    <p class="mx-auto mt-2 max-w-xl text-sm text-slate-500">
        Nothing in the system can serve a document file today. The upload, submission and file-serving
        path is specified and lands as its own change — until it does, this tab has nothing it could
        honestly show.
    </p>
</div>
